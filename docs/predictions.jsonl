{"check": "After the next 10 SKILL.md writes, count hook blocks (exit 2) vs skill_lint --strict failures in CI. Hook blocks > 0 and CI ERROR failures == 0 supports it.", "claim": "Moving skill_lint from CI-only to a PreToolUse hook catches at least one frontmatter or naming ERROR at write time that CI would otherwise have surfaced a commit later. If ten SKILL.md edits pass with zero hook blocks, the hook is ceremony and should be reverted to CI-only.", "date": "2026-08-16", "id": "skill-lint-hook", "kind": "prediction"}
{"check": "skill_lint --strict stays clean, and over the next 5 authoring sessions no session needs a second attempt because the wrong skill loaded.", "claim": "Merging author-skill and session-to-skill removes a routing ambiguity rather than just renaming one. Expect skill-craft to be invoked for both authoring and distilling without the user naming a mode, and expect no new overlap finding between skill-craft and any other skill.", "date": "2026-08-16", "id": "skill-craft-merge", "kind": "prediction"}
{"check": "Count red CI runs on dev over the next 10 merges to dev.", "claim": "Running CI on dev rather than main-only catches at least one failure on dev before it reaches main within the next 10 merges. If every dev run is green and main never regressed anyway, the extra CI is cost without signal.", "date": "2026-08-16", "id": "ci-on-dev", "kind": "prediction"}
{"check": "py -3.11 -m pytest -q collects and passes; the new syntax-floor test enumerates every shipped script and reports how many failed ast.parse at feature_version=(3,9) before the fix.", "claim": "A mechanical 3.9-syntax guard plus a CI Python matrix will catch version-floor breakage that the OS matrix alone never could; I expect it to find at least one further 3.12-only construct beyond site.py:384 in the currently shipped scripts.", "date": "2026-08-16", "id": "adr-003-decompose", "kind": "prediction"}
{"date": "2026-08-16", "evidence": "First half RIGHT: 3.11 went from cannot-collect to 785 passing, and the OS matrix could never have shown it (all three CI jobs pinned 3.12). Second half WRONG as stated: there was exactly ONE further 3.12-only construct, not more - the whole repo had a single one. But a second floor violation of a different kind did exist, 'import tomllib' at module scope (3.11 stdlib, not syntax), which the prediction did not anticipate because it assumed the problem was syntactic. The most useful outcome is that the CHECK I specified was invalid: ast.parse at feature_version=(3,9) reports the pre-fix file as CLEAN, because PEP 701 is a tokenizer change and feature_version only gates grammar. I had written down a measurement instrument that cannot measure the thing. The working guard needed three passes (tokenize, feature_version, AST import scan) plus a real-interpreter CI matrix.", "id": "adr-003-decompose", "kind": "outcome", "verdict": "partial"}
{"check": "Read the python-floor matrix results on the PR into dev: four jobs (3.9, 3.10, 3.11, 3.13). Record which interpreters failed and on what — a SyntaxError means python_floor.py missed something static and must be extended; a runtime TypeError/AttributeError means the static checker is working as designed and the gap is inherent to static checking.", "claim": "The new python-floor CI job will FAIL on Python 3.9 and/or 3.10 on its first run, because python_floor.py checks syntax and stdlib imports but cannot see runtime API usage (dict | merge, zip(strict=), 3.10-only kwargs). I grepped for the common ones and found none, but a grep is not an interpreter. I put this at roughly 40 percent. If it passes on all four, the 3.9+ promise is proven across its full declared range for the first time; if it fails, the gate caught on its first real outing exactly the class of defect it was built for, and the fix is a decision (patch the calls, or raise the floor to 3.11 and say so in the README) rather than a scramble.", "date": "2026-08-16", "id": "adr-003-ci-floor-matrix", "kind": "prediction"}
{"date": "2026-08-16", "evidence": "3.9 FAILED on first run; 3.10, 3.11 and 3.13 passed. Predicted '3.9 and/or 3.10' at ~40 percent - right, and right about which end of the range. But the MECHANISM was wrong in an instructive way. I predicted a runtime API failure in shipped code (dict | merge, zip(strict=)). Shipped code was fine: every other test collected and passed on 3.9, so the repo does honour its declared floor. What failed was my own test, asserting that --floor 3.10 ACCEPTS a match statement - which is false when the host is 3.9, because ast.parse feature_version can only make the parser stricter, never grant it grammar the host lacks. It is not a version simulator. That is the THIRD distinct way this one API misled me in a single session (the other two: PEP 701 is a tokenizer change so feature_version misses it entirely; and tomllib is not a syntax error at all). All three are now pinned by tests. Net: the matrix earned its place on its first run, catching a real defect that no local interpreter I had could show.", "id": "adr-003-ci-floor-matrix", "kind": "outcome", "verdict": "right"}
{"check": "git clone --no-hardlinks . <tmp>/fresh && run check.py --repo on both the working copy and <tmp>/fresh; diff the archive-candidate finding sets. Pre-fix: fresh clone yields zero archive findings while the working copy yields some. Post-fix: the two sets are identical.", "claim": "Replacing mtime with git committer date at all four archive-rule sites makes cms archive findings clone-stable: a fresh clone of hephaestus and this working copy will report an identical set of archive-candidate findings. Today they disagree, because a clone resets every mtime to checkout time and every doc reads as 0 days old.", "date": "2026-08-22", "id": "clone-stable-doc-age", "kind": "prediction"}
{"check": "Count dirs matching hephaestus-*-state-* in %TEMP%, run 'python -m pytest -q' three times, count again. Pre-fix the count rises by 6; post-fix it is unchanged.", "claim": "Guarding the mkdtemp calls behind an is-it-already-set check and adding pytest_unconfigure teardown reduces the per-pytest-process temp-dir leak from 2 dirs to 0. The 468 stray dirs already in %TEMP% are historical and will not grow.", "date": "2026-08-22", "id": "conftest-temp-dir-leak", "kind": "prediction"}
{"date": "2026-08-22", "evidence": "Measured directly. Pre-fix, three pytest runs took the stray-dir count from 245 to 248 for each of the two prefixes (+2 per process, exactly as predicted). Post-fix, three FULL suite runs left it at 251/251 - delta 0. Predicted 2 -> 0; observed 2 -> 0. The historical dirs (490 across both prefixes at the time of writing, higher than the 468 in the handoff because the leak was still running) did not grow. Caveat on scope, not on the verdict: teardown removes only dirs the process created, so a caller-supplied CMS_STATE_DIR is never touched, and that is pinned by a test rather than left to the docstring.", "id": "conftest-temp-dir-leak", "kind": "outcome", "verdict": "right"}
{"date": "2026-08-22", "evidence": "Mechanism RIGHT, empirical claim WRONG, and the check I wrote down was invalid on the repo I named it against. The clone-stability property is real and demonstrated: tests/test_doc_age.py::test_a_fresh_clone_agrees_with_the_original builds a repo with a 200-day-old and a 3-day-old committed plan, clones it, and asserts both report an IDENTICAL finding set - one finding, the old one. That test fails under the mtime implementation, which is what makes it worth having. But the claim's second sentence, 'Today they disagree', was simply false about hephaestus, and I could have known before recording it: hephaestus contains ZERO files matching ARCHIVE_PATTERNS, so the rule never fires here at all. Working copy and fresh clone both reported 0 archive findings before the fix and both report 0 after. Running the recorded check verbatim on the named repo yields 0 == 0 pre- and post-fix - it cannot distinguish a working implementation from a broken one. This is the third time in this ledger that the specified instrument, rather than the claim, was the defective part (see adr-003-decompose and adr-003-ci-floor-matrix, both of which turned on ast.parse feature_version not measuring what I assumed). The transferable lesson is narrower than 'check your instrument': before recording a check, RUN it against the unfixed tree and confirm it currently FAILS. A check that passes pre-fix is not a check. Thirty seconds would have caught this one.", "id": "clone-stable-doc-age", "kind": "outcome", "verdict": "partial"}
{"check": "Two commands, both of which I ran against the unfixed tree first and both of which currently FAIL to catch the thing (recorded baselines below). (1) predictions.py record <id> --claim c --check k --date d with no --baseline: pre-fix exit 0, post-fix exit 1. (2) Re-introduce a path.stat().st_mtime read into plugins/crucible/skills/cms/scripts/check.py and run invariants_run.py: pre-fix exit 0, post-fix exit 1. Neither is a future-facing guess; both are runnable now.", "claim": "Enforcing a stated baseline in predictions.py record, and pinning the mtime lesson as an AST-based invariant, converts two lessons that were previously prose into gates that fail. Specifically: record will refuse a prediction that does not state what its check shows on the unfixed tree, and invariants_run will fail if any shipped plugin script reads st_mtime outside the documented allowlist.", "date": "2026-08-22", "id": "check-validity-gates", "kind": "prediction"}
{"date": "2026-08-22", "evidence": "Both checks ran, both were confirmed failing on the unfixed tree first (that baseline is recorded on the prediction itself), and both now catch what they were built for. (1) predictions.py record with no --baseline: pre-fix exit 0 and the prediction was silently accepted; post-fix exit 1 with the explanation. (2) Reintroducing path.stat().st_mtime into check.py: pre-fix invariants_run reported '3 passed, 0 failed' and exited 0; post-fix it reports 'FAIL mtime is never a clock in shipped plugin code', '3 passed, 1 failed', and exits 1. Restored, back to 4 passed / exit 0. Two things I did not predict, both found by running rather than reading: the --check refusal message had been unreachable since it was written, because required=True makes argparse exit 2 - the code this script documents as 'store unreadable or malformed' - so a forgotten flag was indistinguishable from a corrupt ledger and the explanation had never been shown to anyone; and a naive grep-based mtime guard would have flagged doc_age.py, the module that FIXED the bug, because its docstring discusses st_mtime at length, which is why the check parses instead. Caveat on scope: this settles that the gates fire, not that they change behaviour over time - whether the baseline rule actually prevents a future invalid check is only answerable after several more predictions, and curate should revisit it.", "id": "check-validity-gates", "kind": "outcome", "verdict": "right"}
{"baseline": "Ran it on the unfixed tree just now: 40 docs x ~1200 bytes = ~48000 bytes aggregate, grep for 'aggregate' and 'total' in check.py output returns NOTHING. The summary line reads '8 error(s), 2 warning(s), 1 info(s)' and none of those concern the aggregate - no aggregate check exists at all, so the check cannot currently distinguish a bounded corpus from an unbounded one.", "check": "Build a fixture repo with 40 docs each carrying a ~1200-byte ai_summary (each well under AI_SUMMARY_MAX_BYTES=1500, aggregate ~48000 bytes) and run check.py on it. Post-fix it must emit exactly one aggregate warning naming the total and the threshold, while emitting no per-summary size warning - if it warns per row too, the threshold is wrong, not the design.", "claim": "The per-document ai_summary cap bounds one row but not the sum, which is what a triage pass actually pays. Adding an aggregate warning to check.py will fire on a corpus where every individual summary is legal, catching the case the per-row cap structurally cannot see.", "date": "2026-08-22", "id": "ai-summary-aggregate-cap", "kind": "prediction"}
{"date": "2026-08-22", "evidence": "Ran the recorded check exactly as specified. The 40-doc / ~1200-byte fixture now produces exactly one finding: 'ai_summary aggregate is 48080 bytes across 40 document(s) (>20000)', and grepping the same run for a per-summary size warning returns zero - so it catches the case the per-row cap cannot see without double-reporting it, which was the falsifiable part. Pinned by 5 tests including the both-halves case. Two things worth recording beyond the claim. First, real-world calibration: hephaestus's own 11 summaries total 5508 bytes, so the 20000 threshold permits roughly 40 documents at this quality - it is not so tight that ordinary use trips it, which was the risk of picking a number. Second, an off-by-60 in my own test exposed that parse_frontmatter counts the surrounding YAML quotes in the value, so a quoted summary measures 2 bytes more than the same text unquoted; pre-existing behaviour shared with the per-row cap, not worth breaking a published contract over, now pinned by a test so the next person finds the reason rather than the arithmetic. Caveat: this settles that the check fires correctly, NOT that 20000 is the right threshold - only a corpus that actually grows past it will settle that, and curate should revisit.", "id": "ai-summary-aggregate-cap", "kind": "outcome", "verdict": "right"}
{"baseline": "All three measured on the unfixed tree just now. (1) sync.py on a reordered-keys facts file exits 0 - it reports success on a file it could not read. (2) parse_frontmatter returns ({}, 0) for BOTH an unterminated fence and no frontmatter at all; the two conditions are byte-identical to every one of the nine consumers. (3) repo_metrics reports 6 dead_module_candidates - all six verified genuinely imported via 'from <pkg> import <mod>' - and 6 gitignore_gaps including node_modules/dist/build in a repo with no JS and no build step; anchor_score 42 with dead_candidate_penalty 6 and gitignore_gap_penalty 12.", "check": "Three commands, each run against the unfixed tree first (baselines below). (1) sync.py --facts <a file whose keys are reordered>: expect exit 2 post-fix. (2) parse-and-compare an unterminated-frontmatter file against an absent-frontmatter file through check.py: post-fix they must produce DIFFERENT findings. (3) repo_metrics.py on hephaestus: dead_module_candidates must drop to 0 and gitignore_gaps must lose node_modules/dist/build, so anchor_score rises from 42 to at least 58.", "claim": "Three checks currently report success or a plausible number while being unable to do their job, and each will report the failure after this change. sync.py will exit 2 rather than 0 on a facts file it cannot parse; check.py will distinguish frontmatter that failed to parse from frontmatter that is absent; and repo_metrics will stop reporting genuinely-imported modules as dead, raising hephaestus's anchor score by at least the 6-point dead-candidate penalty plus most of the 12-point gitignore penalty.", "date": "2026-08-22", "id": "loud-failures-and-metric-accuracy", "kind": "prediction"}
{"date": "2026-08-22", "evidence": "Retired as unclear because the CHECK CANNOT BE RUN, not because the outcome was ambiguous. The recorded check was 'skill_lint --strict stays clean, and over the next 5 authoring sessions no session needs a second attempt because the wrong skill loaded'. The first clause is satisfied - skill_lint --strict has been clean on every run since, 0 error 0 warn across 17 skills, and no overlap finding has ever named skill-craft. The second clause is unmeasurable: nothing in this repo or in Claude Code records authoring sessions, which skill was loaded for one, or whether a second attempt was needed. There is no instrument and there never was. This is the same invalid-instrument defect that produced two other partial verdicts here, caught retrospectively rather than at record time, and it is precisely what predictions.py record --baseline now prevents: running the check against the unchanged tree would have shown immediately that it produces nothing to compare against. Recording unclear rather than right is the honest call - the merge may well have removed the routing ambiguity, but this ledger has no evidence either way and inferring success from the absence of complaints is how a record like this becomes fiction. If the question is worth re-asking, it needs a new prediction with an instrument that exists.", "id": "skill-craft-merge", "kind": "outcome", "verdict": "unclear"}
{"date": "2026-08-22", "evidence": "All three mechanisms work; one numeric claim was wrong, and being wrong is the informative part. (1) RIGHT: sync.py on a reordered-keys facts file went exit 0 -> exit 2 with 'cannot determine'. (2) RIGHT: an unterminated fence and absent frontmatter now produce different findings ('opens with --- but is never closed' vs 'missing frontmatter'), where both previously produced ({}, 0) and, on an exempt file, silence. (3) PARTIAL: dead_module_candidates went 6 -> 0 exactly as claimed, but anchor_score rose 42 -> 50, NOT to the 'at least 58' I predicted. The gitignore penalty only fell 12 -> 10 because just ONE of the six gaps (node_modules) was actually inapplicable; .coverage, htmlcov, dist and build remain legitimately expected, because this tree contains two real pyproject.toml files (plugins/mcp-starter and plugins/sqlite-readonly server projects) that genuinely can produce build output. So my prior claim - repeated in the curate report and the CHANGELOG - that '18 of 58 penalty points are artifact' was itself wrong: only 8 were (6 dead + 2 gitignore). I had assumed the gitignore gaps were mostly noise without checking which markers were present, which is the same not-running-the-check error this week keeps producing. Both documents corrected rather than left standing. The remaining 40-point god-file penalty is real and untouched.", "id": "loud-failures-and-metric-accuracy", "kind": "outcome", "verdict": "partial"}
